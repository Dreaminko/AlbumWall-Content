name: Trigger Cloudflare Pages Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cloudflare Pages Deploy Hook
        run: |
          curl --fail-with-body --silent --show-error -X POST "${{ secrets.CLOUDFLARE_DEPLOY_HOOK }}"
